import * as React from "react";
import Main from "../components/Create Page/Main";

interface ICreateProps {}

const Create: React.FunctionComponent<ICreateProps> = (props) => {
  return <Main />;
};

export default Create;
